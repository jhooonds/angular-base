import { NgbDate } from '@ng-bootstrap/ng-bootstrap';

export class User {
    idUser: number
    userName: string;
    password: string;
    isEnable: Boolean;
    registerDate: Date;
    name: String;
    surname: String;
    email: String;
    phone: string;
}